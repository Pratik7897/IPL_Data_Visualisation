# Core web framework
dash>=2.14.0
dash-bootstrap-components>=1.5.0
plotly>=5.18.0
flask>=3.0.0
gunicorn>=21.0.0

# Data
pandas>=2.1.0
numpy>=1.26.0

# ML / NLP
transformers>=4.35.0
torch>=2.1.0

# Phase 4 — AI/ML prediction + player index
scikit-learn>=1.4.0
joblib>=1.3.0
xgboost>=2.0.0          # optional — RandomForest used as fallback if missing

# Phase 4 — AI summary providers (all optional — template engine is default)
google-generativeai>=0.5.0    # enable: set GEMINI_API_KEY
openai>=1.12.0                # enable: set OPENAI_API_KEY

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9      # PostgreSQL (Phase 3A) — binary wheel, no libpq build needed

# Visualisation extras
wordcloud>=1.9.3
Pillow>=10.0.0

# Twitter / X API
tweepy>=4.14.0

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0
